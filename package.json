{
  "name": "traceops-dev",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "api",
    "mcp-server"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "dev:api": "npm run start --workspace @traceops/api",
    "dev:mcp": "npm run start --workspace @traceops/mcp-server"
  },
  "engines": {
    "node": ">=20"
  }
}
